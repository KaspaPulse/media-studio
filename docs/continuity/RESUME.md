# Resume existing work

Policy ID: UNIVERSAL_CONTINUITY_V2_20260913

RESUME THE EXISTING WORK. This is not a new audit or new implementation task.

1. Read AGENTS.md, all applicable AGENTS.override.md files and PROJECT_STATE.md
   completely. Read the repository's continuity policy and its interruption-safe
   supplement. Resolve the canonical ledger from REPOSITORY_ADOPTION.md and actual
   project records; do not assume a shared CURRENT belongs to this task.
2. Bind the actual host, repository root, worktree, branch, HEAD, index, dirty and
   untracked work. Inspect active owners/jobs before launching anything.
3. Read the current task checkpoint, latest relevant operation records and evidence.
   Discover/classify pertinent Markdown; read authoritative relevant docs and ACTIVE
   plans, not every Markdown file. Inspect relevant remotes read-only when accessible.
4. Recover undocumented progress: missing checkpoints are not proof of no work.
   Inspect diffs, commits, artifacts, test receipts, logs and existing processes,
   plus runtime/database state only when relevant and authorized.
5. Resolve every uncertain interrupted operation's actual outcome before retrying.
   Preserve partial work and evidence. Never reset, clean, discard, overwrite,
   auto-pull/merge/rebase, or replay an old NEXT ACTION blindly.
6. Classify verified, unverified, running, partial, failed, unknown, blocked and
   remaining work. Reuse verified results whose source/environment scope still matches.
7. Persist the reconciled checkpoint and a write-ahead intent for the next material
   action. CONTINUE THE ACTUAL AUTHORIZED WORK from the first unfinished safe boundary;
   do not merely summarize or ask the owner to repeat recoverable facts.
8. Record verified outcomes immediately after material actions and milestones.
   Retain unknown outcomes as UNKNOWN. End with exact state, evidence, remaining work
   and a durable next action; do not claim push/deploy authorization from this prompt.

When recovery exposes a real ownership, safety or authorization blocker, record the
specific blocker and stop only the affected action rather than silently repeating it.
